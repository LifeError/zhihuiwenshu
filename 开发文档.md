# RAG智能问答系统 - 详细开发文档

version：0.1.1
date：2026-05-11

---

## 一、项目概述

本项目是一个基于 **RAG（Retrieval-Augmented Generation）** 架构的智能问答系统，支持PDF/MD文档的知识库导入、向量化存储、多路召回检索、重排序以及LLM答案生成。系统采用 **LangGraph** 作为工作流编排引擎，**FastAPI** 提供Web服务接口。

### 1.1 核心能力

| 能力 | 描述 |
|------|------|
| 文档导入 | 支持PDF/MD文件上传，自动解析、分块、向量化并存入Milvus |
| 智能问答 | 多路召回（向量检索 + HyDE + 网络搜索）→ RRF融合 → Rerank精排 → LLM生成 |
| 主体识别 | 自动识别用户问题中的产品/设备名称，精准定位知识库范围 |
| 流式输出 | 支持SSE流式推送答案，实时展示处理进度 |
| 历史对话 | MongoDB存储对话记录，支持上下文关联的多轮对话 |

### 1.2 技术栈

| 技术 | 用途 | 版本 |
|------|------|------|
| Python | 开发语言 | ≥3.12 |
| FastAPI | Web服务框架 | ≥0.136.1 |
| LangGraph | 工作流编排 | ≥1.1.9 |
| LangChain | LLM调用封装 | ≥1.2.15 |
| Milvus | 向量数据库 | ≥2.6.12 |
| BGE-M3 | Embedding模型 | 本地部署 |
| FlagEmbedding | Reranker模型 | ≥v1.3.5 |
| MongoDB | 对话历史存储 | ≥4.17.0 |
| MinIO | 文件/图片存储 | ≥7.2.20 |
| MinerU | PDF解析 | ≥1.3.12 |
| Loguru | 日志系统 | ≥0.7.3 |
| OpenAI Agents | MCP网络搜索 | ≥0.4.2 |

---

## 二、项目目录结构

```
rag_project/
├── main.py                              # 入口文件（占位，实际服务由各子模块启动）
├── pyproject.toml                       # 项目依赖配置
├── .env                                 # 环境变量配置（API密钥、数据库地址等）
│
├── evaluation_data/                     # 【评估数据】标准问答对JSON文件
│   ├── query_answer.json                # 标准问答对（鲸鱼系列打印机，20条）
│   └── query_answer1.json               # 标准问答对（Aolynk CB304n网桥，15条）
│
├── doc/                                 # 【数据文件夹】PDF/MD知识库源文件（80+个PDF）
│   ├── *.pdf                            # 各类产品用户手册PDF
│   └── project_plan.xlsx
│
├── logs/                                # 日志输出目录
│   └── app_YYYYMMDD.log                 # 按日期分割的日志文件
│
├── output/                              # 文档处理中间输出目录
│   └── YYYYMMDD/                        # 按日期组织
│       └── {task_id}/                   # 每个导入任务独立目录
│           ├── *.md                     # 转换后的Markdown
│           ├── *_content_list.json      # MinerU解析元数据
│           ├── *_model.json             # 模型识别结果
│           ├── chunks.json              # 文档分块结果
│           └── images/                  # 提取的图片
│
├── prompts/                             # 提示词模板目录
│   ├── answer_out.prompt                # 答案润色生成提示词
│   ├── hyde_prompt.prompt               # HyDE假设性答案生成提示词
│   ├── image_summary.prompt             # 图片摘要提示词
│   ├── item_name_recognition.prompt     # 主体名称识别提示词
│   ├── product_recognition_system.prompt # 产品识别系统提示词
│   ├── relevance_judge.prompt           # 文档相关性判断提示词（评估模块）
│   ├── faithfulness_evaluation.prompt   # Faithfulness评估提示词（评估模块）
│   ├── answer_relevancy_evaluation.prompt # Answer Relevancy评估提示词（评估模块）
│   └── rewritten_query_and_itemnames.prompt # 问题重写+主体提取提示词
│
├── app/                                 # 核心应用代码
│   ├── core/                            # 核心工具模块
│   │   ├── logger.py                    # 日志工具（loguru封装，支持控制台+文件双输出）
│   │   └── load_prompt.py              # 提示词加载器（支持变量渲染）
│   │
│   ├── conf/                            # 配置模块（从.env读取）
│   │   ├── bailian_mcp_config.py        # 百炼MCP网络搜索配置
│   │   ├── embedding_config.py          # BGE-M3 Embedding配置
│   │   ├── lm_config.py                 # LLM大模型配置
│   │   ├── milvus_config.py             # Milvus向量库配置
│   │   ├── mineru_config.py             # MinerU PDF解析配置
│   │   ├── minio_config.py              # MinIO对象存储配置
│   │   └── reranker_config.py           # Reranker重排序模型配置
│   │
│   ├── clients/                         # 外部服务客户端
│   │   ├── milvus_utils.py              # Milvus操作（混合搜索、批量查询、客户端单例）
│   │   ├── minio_utils.py               # MinIO文件上传/下载
│   │   ├── mongo_history_utils.py       # MongoDB对话历史CRUD
│   │   ├── mongo_history_utils_new.py   # MongoDB新版工具（预留）
│   │   └── neo4j_utils.py              # Neo4j知识图谱操作
│   │
│   ├── lm/                              # 语言模型工具模块
│   │   ├── embedding_utils.py           # BGE-M3向量生成（稠密+稀疏，单例模式）
│   │   ├── lm_utils.py                  # LLM客户端（OpenAI兼容API，缓存单例）
│   │   └── reranker_utils.py           # Reranker模型加载（FlagEmbedding，单例）
│   │
│   ├── utils/                           # 通用工具模块
│   │   ├── escape_milvus_string_utils.py # Milvus字符串转义
│   │   ├── format_utils.py              # 格式化工具
│   │   ├── normalize_sparse_vector.py   # 稀疏向量归一化
│   │   ├── path_util.py                 # 路径工具（项目根目录定位）
│   │   ├── rate_limit_utils.py          # 速率限制工具
│   │   ├── sse_utils.py                 # SSE服务端推送工具
│   │   └── task_utils.py               # 任务状态管理（内存字典，进度追踪）
│   │
│   ├── import_process/                  # 【知识库导入流程】
│   │   ├── agent/
│   │   │   ├── main_graph.py            # LangGraph导入工作流定义
│   │   │   ├── state.py                 # 导入流程状态定义(ImportGraphState)
│   │   │   └── nodes/                   # 导入流程各节点
│   │   │       ├── node_entry.py        # 入口节点：参数校验、文件类型判断
│   │   │       ├── node_pdf_to_md.py    # PDF→Markdown转换（MinerU）
│   │   │       ├── node_md_img.py       # MD图片处理（提取/下载/修复路径）
│   │   │       ├── node_document_split.py # 文档分块（按语义切分）
│   │   │       ├── node_item_name_recognition.py # 主体名称识别
│   │   │       ├── node_bge_embedding.py # BGE-M3向量化
│   │   │       └── node_import_milvus.py # 导入Milvus向量库
│   │   ├── api/
│   │   │   └── import_server.py         # FastAPI导入服务（端口8000）
│   │   └── page/
│   │       └── import.html              # 导入前端页面
│   │
│   ├── query_process/                   # 【智能问答流程】
│   │   ├── agent/
│   │   │   ├── main_graph.py            # LangGraph查询工作流定义
│   │   │   ├── state.py                 # 查询流程状态定义(QueryGraphState)
│   │   │   └── nodes/                   # 查询流程各节点
│   │   │       ├── node_item_name_confirm.py  # 主体确认+问题重写
│   │   │       ├── node_search_embedding.py   # 向量检索（稠密+稀疏混合）
│   │   │       ├── node_search_embedding_hyde.py # HyDE假设性答案检索
│   │   │       ├── node_web_search_mcp.py     # MCP网络搜索
│   │   │       ├── node_rrf.py                # RRF倒数排名融合
│   │   │       ├── node_rerank.py             # Rerank精排+断崖截断
│   │   │       └── node_answer_output.py      # 答案生成+输出
│   │   ├── api/
│   │   │   └── query_server.py          # FastAPI查询服务（端口8001）
│   │   ├── page/
│   │   │   ├── chat.html                # 对话前端页面
│   │   │   └── query_monitor.html       # 查询监控页面
│   │   └── sse/
│   │       ├── sse_step_1.html/py       # SSE示例1
│   │       └── sse_step_2.html/py       # SSE示例2
│   │
│   ├── test/                            # 测试模块
│   │   ├── test_import_main_graph.py    # 导入流程测试
│   │   ├── test_milvus_client.py        # Milvus客户端测试
│   │   └── test_query_main_graph.py     # 查询流程测试
│   │
│   ├── evaluate/                         # 【RAG评估模块】
│   │   ├── __init__.py                   # 模块初始化+导出
│   │   ├── evaluation_config.py          # 评估配置（动态模板，支持切换问答JSON/输出目录）
│   │   ├── main_graph_evaluation.py      # 评估专用LangGraph工作流（无联网搜索）
│   │   ├── retrieval_evaluator.py        # 检索评估器（LLM相关性判断+四个指标）
│   │   ├── generation_evaluator.py       # 生成评估器（Faithfulness/Answer Relevancy，完整返回claims/brief_reason）
│   │   ├── evaluation_runner.py          # 评估运行器（编排+日志输出，含brief_reason/claims统计）
│   │   └── evaluation_server.py          # FastAPI评估服务接口（端口8002）
│   │
│   └── tool/                            # 工具脚本
│       ├── download_bgem3.py            # BGE-M3模型下载
│       └── download_reranker.py         # Reranker模型下载
```

---

## 三、核心架构详解

### 3.1 查询工作流（Query Graph）

```
┌──────────────────────────────────────────────────────────────────┐
│                        Query Workflow                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────────┐                                         │
│  │ node_item_name_     │  ① LLM提取主体名称 + 重写问题            │
│  │ confirm             │  ② Milvus向量库确认主体                  │
│  └────────┬────────────┘  ③ 分类：确认/可选/无匹配               │
│           │                                                       │
│           ▼ (条件路由)                                            │
│     ┌─────┴─────┐                                                │
│     │ answer?   │                                                │
│     └─┬───┬───┬─┘                                                │
│  YES  │   │   │  NO (并行三路召回)                                │
│       │   │   │                                                   │
│       │   ▼   ▼          ▼                                       │
│       │ ┌──────────┐ ┌──────────────────┐ ┌──────────────────┐  │
│       │ │node_     │ │node_search_      │ │node_web_         │  │
│       │ │search_   │ │embedding_hyde    │ │search_mcp        │  │
│       │ │embedding │ │(HyDE假设性答案)   │ │(MCP网络搜索)     │  │
│       │ └────┬─────┘ └────────┬─────────┘ └────────┬─────────┘  │
│       │      │                │                     │             │
│       │      └────────────────┼─────────────────────┘             │
│       │                       ▼                                   │
│       │              ┌──────────────────┐                        │
│       │              │    node_rrf      │  RRF倒数排名融合        │
│       │              │  (同源融合排序)   │  公式: Σ(1/(60+rank))*w│
│       │              └────────┬─────────┘                        │
│       │                       ▼                                   │
│       │              ┌──────────────────┐                        │
│       │              │   node_rerank    │  Cross-Encoder精排      │
│       │              │ (打分+断崖截断)   │  + 动态TopK算法         │
│       │              └────────┬─────────┘                        │
│       │                       │                                   │
│       └───────────────────────┘                                   │
│                               ▼                                   │
│                    ┌──────────────────┐                          │
│                    │node_answer_output│  Prompt组装→LLM生成       │
│                    │ (答案生成+输出)   │  流式SSE/同步返回         │
│                    └──────────────────┘                          │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

### 3.2 状态数据结构（QueryGraphState）

```python
class QueryGraphState(TypedDict):
    session_id: str              # 会话唯一标识
    original_query: str          # 用户原始问题
    
    # 检索中间数据
    embedding_chunks: list       # 普通向量检索切片
    hyde_embedding_chunks: list  # HyDE检索切片
    web_search_docs: list        # 网络搜索文档
    
    # 排序数据
    rrf_chunks: list             # RRF融合排序后切片
    reranked_docs: list          # 重排序后最终Top-K文档
    
    # 生成数据
    prompt: str                  # 组装好的Prompt
    answer: str                  # 最终生成的答案
    
    # 辅助信息
    item_names: List[str]        # 提取的商品名称
    rewritten_query: str         # 改写后的问题
    history: list                # 历史对话记录
    is_stream: bool              # 是否流式输出
```

### 3.3 检索流程详解

| 步骤 | 节点 | 功能 | 关键技术 |
|------|------|------|----------|
| 1 | node_item_name_confirm | 主体确认+问题重写 | LLM提取→Milvus向量确认→分类 |
| 2a | node_search_embedding | 向量检索 | BGE-M3稠密+稀疏混合搜索 |
| 2b | node_search_embedding_hyde | HyDE检索 | LLM生成假设答案→向量搜索 |
| 2c | node_web_search_mcp | 网络搜索 | 百炼MCP Streamable HTTP |
| 3 | node_rrf | 同源融合排序 | Reciprocal Rank Fusion |
| 4 | node_rerank | 精排+截断 | Cross-Encoder打分+断崖算法 |
| 5 | node_answer_output | 答案生成 | Prompt组装→LLM流式/同步输出 |

### 3.4 关键配置项（.env）

| 配置项 | 说明 | 示例值 |
|--------|------|--------|
| LLM_DEFAULT_MODEL | 默认LLM模型 | qwen-flash |
| OPENAI_API_KEY | API密钥 | sk-xxx |
| OPENAI_BASE_URL | API地址 | https://dashscope.aliyuncs.com/... |
| BGE_M3_PATH | Embedding模型路径 | E:\temp\ai_models\...\bge-m3 |
| MILVUS_URL | Milvus地址 | http://127.0.0.1:19530 |
| CHUNKS_COLLECTION | 切片集合名 | kb_chunks |
| ITEM_NAME_COLLECTION | 主体集合名 | kb_item_names |
| MONGO_URL | MongoDB地址 | mongodb://127.0.0.1:27017 |
| BGE_RERANKER_LARGE | Reranker模型路径 | E:\temp\ai_models\...\bge-reranker-large |

**评估模块配置项（.env）**：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| EVALUATION_LLM_MODEL | 评估用LLM模型 | qwen-flash |
| EVALUATION_LLM_TEMPERATURE | 评估LLM温度 | 0.0 |
| RETRIEVAL_TOP_K | 检索TopK值 | 5 |

> 注：`EVALUATION_OUTPUT_DIR`、`QUERY_ANSWER_PATH`、`DEFAULT_K_VALUES` 不在 `.env` 中配置，通过 `evaluation_config` 的动态方法（`set_query_answer_path()`、`set_output_dir()`、`update_config()`）在运行时设置。

---

## 四、关键模块详解

### 4.1 日志系统（app/core/logger.py）

- 基于 **loguru** 实现
- 支持 `.env` 配置控制台/文件双输出
- 自动按日期分割日志文件（`logs/app_YYYYMMDD.log`）
- 自动清理过期日志（默认保留7天）
- 支持异步写入（`enqueue=True`）
- 精准定位业务代码调用位置

### 4.2 LLM客户端（app/lm/lm_utils.py）

- 基于 **LangChain ChatOpenAI** 封装
- 全局缓存单例模式（按模型+JSON模式缓存）
- 支持JSON标准化输出模式
- 适配千问/即梦等国产模型的私有参数

### 4.3 Embedding（app/lm/embedding_utils.py）

- 使用 **BGE-M3** 模型
- 同时生成稠密向量（1024维）+ 稀疏向量
- 模型原生L2归一化，适配Milvus IP内积检索
- 单例模式避免重复加载

### 4.4 Milvus客户端（app/clients/milvus_utils.py）

- 全局单例连接
- 支持稠密+稀疏向量混合搜索（WeightedRanker加权融合）
- 支持过滤表达式（`expr`）精准筛选
- 支持通过chunk_id批量查询

### 4.5 任务状态管理（app/utils/task_utils.py）

- 内存字典管理任务状态
- 支持：pending → processing → completed/failed
- 节点中英文名映射
- SSE实时推送进度

---

## 五、API接口一览

### 5.1 导入服务（端口8000）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/import` | 返回导入页面HTML |
| POST | `/upload` | 上传文件并异步开启导入流程 |
| GET | `/status/{task_id}` | 查询任务处理进度 |

### 5.2 查询服务（端口8001）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |
| GET | `/chat.html` | 返回对话页面HTML |
| POST | `/query` | 发起提问（支持同步/流式） |
| GET | `/stream/{session_id}` | SSE流式获取答案 |
| GET | `/history/{session_id}` | 查询历史对话 |
| DELETE | `/history/{session_id}` | 清空历史对话 |

---

## 六、数据流关键路径

### 6.1 同步查询完整链路

```
用户提问 → /query API
  → create_query_default_state(session_id, original_query)
  → query_app.invoke(state)
    → node_item_name_confirm: LLM提取主体+重写问题 → Milvus确认
    → node_search_embedding: 问题→向量→Milvus混合搜索→embedding_chunks
    → node_search_embedding_hyde: LLM生成假设答案→向量搜索→hyde_embedding_chunks
    → node_web_search_mcp: MCP网络搜索→web_search_docs
    → node_rrf: 同源RRF融合→rrf_chunks
    → node_rerank: 合并+Cross-Encoder打分+断崖截断→reranked_docs
    → node_answer_output: Prompt组装→LLM生成→set_task_result("answer")
  → get_task_result(session_id, "answer")
  → 返回JSON {answer, session_id}
```

### 6.2 流式查询链路

```
用户提问 → /query API (is_stream=True)
  → create_sse_queue(session_id)
  → background_tasks: run_query_graph(...)
  → 立即返回 {session_id, message: "处理中..."}
  
前端 → /stream/{session_id} (SSE长连接)
  → sse_generator 持续从队列读取事件
  → progress事件: {status, done_list, running_list}
  → delta事件: {delta: "逐字推送的答案"}
  → final事件: {answer, status:"completed", image_urls}
```

---

## 七、evaluation_data/ 目录说明

该目录存放标准问答对JSON文件，用于RAG评估模块：

```
evaluation_data/
├── query_answer.json                   # 标准问答对（鲸鱼系列打印机，20条）
└── query_answer1.json                  # 标准问答对（Aolynk CB304n网桥，15条）
```

**JSON结构**：
```json
[
    {
        "query": "用户问题",
        "answer": "标准答案"
    }
]
```

**评估模块使用方式**：
- 通过 `evaluation_config.set_query_answer_path(path)` 动态切换问答文件
- 通过 `evaluation_config.set_output_dir(path)` 动态切换输出日志目录
- 通过 `evaluation_config.update_config(**kwargs)` 批量更新配置
- 通过 `evaluation_config.reset_to_defaults()` 恢复默认配置
- 通过 `create_evaluation_config(...)` 创建独立配置实例（不影响全局单例）
- 评估时，LLM根据标准答案逐篇判断检索文档的相关性（`{"relevant": true/false}`）
- 四个检索指标（Hit@K、MRR、Context Recall、Context Precision）统一基于LLM相关性判断结果构建

**评估工作流**：
- 检索评估：复用查询工作流的检索部分（node_item_name_confirm → embedding + hyde → rrf → rerank），不执行答案生成
- 生成评估：使用评估专用工作流 `evaluation_query_app`（**无联网搜索**），完整走检索+生成链路
- 评估专用工作流与原查询工作流的区别：去掉 `node_web_search_mcp` 节点，确保评估结果不受联网搜索变量干扰

**生成评估日志输出字段**：
- Faithfulness：`faithfulness_score`（评分）、`total_claims`（总声明数）、`supported_claims`（被支持声明数）、`brief_reason`（评估理由）、`claims`（逐声明详情）
- Answer Relevancy：`relevancy_score`（评分）、`brief_reason`（评估理由）

**query_answer.json**（20条）所有问题均围绕"联想鲸鱼系列打印机"产品，涵盖：
- 纸张尺寸、电源要求
- 打印头安装、墨水瓶安装
- 网络配置、打印作业取消
- 身份证复印、扫描格式
- 卡纸处理、墨水更换
- 清洁维护、固件升级
- 无边距打印、校准操作

**query_answer1.json**（15条）所有问题均围绕"Aolynk CB304n Cable网桥"产品，涵盖：
- 产品类型、接口数量
- IP地址、登录凭据
- 网络连通性检查、无线网络配置
- DHCP、MAC地址过滤
- 恢复出厂设置、DDNS
- 故障排查、传输速率

---

## 八、扩展点说明

| 扩展方向 | 涉及模块 | 说明 |
|----------|----------|------|
| 新增检索路 | query_process/agent/nodes/ | 添加新节点+注册到main_graph |
| 新增评估功能 | 新建app/evaluate/ | 独立模块，不修改现有架构；支持动态切换问答JSON文件 |
| 新增LLM模型 | app/lm/lm_utils.py | 修改get_llm_client缓存逻辑 |
| 新增向量库 | app/clients/ | 添加新客户端工具类 |
| 新增配置项 | app/conf/ + .env | 添加配置类+环境变量 |

---

## 九、依赖关系图

```
main.py (入口)
  └── app/
       ├── core/ (logger, load_prompt) ← 被所有模块依赖
       ├── conf/ (配置类) ← 被lm/, clients/ 依赖
       ├── lm/ (embedding_utils, lm_utils, reranker_utils)
       │    └── 依赖 conf/ (lm_config, embedding_config, reranker_config)
       ├── clients/ (milvus_utils, mongo_history_utils, minio_utils)
       │    └── 依赖 conf/ (milvus_config, minio_config)
       ├── utils/ (task_utils, sse_utils, path_util)
       │    └── 依赖 core/logger
       ├── import_process/ (导入工作流)
       │    └── 依赖 core/, conf/, lm/, clients/, utils/
       └── query_process/ (查询工作流)
            └── 依赖 core/, conf/, lm/, clients/, utils/
```
